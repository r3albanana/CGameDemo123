#include <stdio.h>
#include <stdlib.h>
#include <macros.h>
#include <array.h>

void collapseFloor(char ** arr, int * row, int * col, int rowSize, int colSize)
{
    if(input == KEY_UP || KEY_DOWN || KEY_LEFT || KEY_RIGHT)
    {
        collapsingFloor(arr, *row, *col);
    }
}